path('<replace_path>', path);
year='<replace_year>'; day='<replace_day>'; sensor='<replace_sensor>';

indir='<replace_datadir>', bicdir='<replace_bicdir>'; region='<replace_region>';
rewrite='<replace_rewrite>'

l2p2bic(sensor,region,indir,bicdir,year,day,rewrite);
